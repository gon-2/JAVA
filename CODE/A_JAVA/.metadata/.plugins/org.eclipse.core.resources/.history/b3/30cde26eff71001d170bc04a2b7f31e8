package com.mc.a_modifier;

public class B_static {

	public static void main(String[] args) {
		// static : [클래스]의 의미를 지니는 제한자
		
		//변수앞에 static를 붙이면 해당 변수는 자바 JVM의 Static영역에 올라가게 된다.
		// static영역에 올라간 데이터는 클래스로더가 클래스를 메모리에 올릴때 까지 올라가게 되며
		// 프로그램이 종료될 때 까지 메모리에서 내려오지 않는다.
		
		// static변수나 메서ㅏ드는 Type,이름 형태로 사용할 수 있따.
		
		// 필드변수
		//  - 인스턴스 변수
		//  - 클래스 변수
	}
}
